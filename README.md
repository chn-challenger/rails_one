### Installation

* Clone the repo
```sh
$ git clone
```

* Install gems (packages)
```sh
$ bundle install
```

* Start server
```sh
$ rails s
```

sddf

* Visit localhost homepage at [http://localhost:3000/restaurants]
